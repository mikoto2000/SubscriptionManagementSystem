<%= form_with(model: subscriber_payment, html: { class: %w[form-control form-control-sm] }) do |form| -%>
  <%- if subscriber_payment.errors.any? -%>
  <div style="color: red">
    <h2><%= t("errors.template.header", model: SubscriberPayment.model_name.human, count: subscriber_payment.errors.count) %></h2>

    <ul>
    <% subscriber_payment.errors.each do |error| %>
      <li><%= error.full_message %></li>
    <%- end -%>
    </ul>
  </div>
  <%- end -%>

  <%- if subscriber_payment.persisted? -%>
  <div>
    <%= form.label :id, style: "display: block" %>
    <div class="ps-2">
      <%= form.number_field :id, class: %w[form-control form-control-sm], disabled: true %>
    </div>
  </div>
  <%- end -%>
  <div>
    <%= form.label :month_for_payment, style: "display: block" %>
    <div class="ps-2">
      <%= form.date_field :month_for_payment, class: %w[form-control form-control-sm] %>
    </div>
  </div>
  <div>
    <%= form.label :payment_date, style: "display: block" %>
    <div class="ps-2">
      <%= form.date_field :payment_date, class: %w[form-control form-control-sm] %>
    </div>
  </div>
  <div>
    <%= form.label SubscriberPayment.reflect_on_association(:payment_status).foreign_key %>
    <div class="ps-2">
      <%= form.collection_select(
            SubscriberPayment.reflect_on_association(:payment_status).foreign_key,
            SubscriberPayment.reflect_on_association(:payment_status).klass.all,
            :id,
            :name,
            { include_blank: "選択なし" }
          ) %>
    </div>
  </div>
  <div>
    <%= form.label SubscriberPayment.reflect_on_association(:subscriber).foreign_key %>
    <div class="ps-2">
      <%= form.collection_select(
            SubscriberPayment.reflect_on_association(:subscriber).foreign_key,
            SubscriberPayment.reflect_on_association(:subscriber).klass.all,
            :id,
            :name,
            { include_blank: "選択なし" }
          ) %>
    </div>
  </div>
  <%- if subscriber_payment.persisted? -%>
  <div>
    <%= form.label :created_at, style: "display: block" %>
    <div class="ps-2">
      <%= form.datetime_field :created_at, class: %w[form-control form-control-sm], disabled: true %>
    </div>
  </div>
  <div>
    <%= form.label :updated_at, style: "display: block" %>
    <div class="ps-2">
      <%= form.datetime_field :updated_at, class: %w[form-control form-control-sm], disabled: true %>
    </div>
  </div>
  <%- end -%>
  <div>
    <%= form.submit class: %w[btn btn-primary] %>
  </div>
<% end %>
